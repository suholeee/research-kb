# Theory

Grounded theory pages go here. In the private KB, theory pages may be organized into `theory/global/` and `theory/cluster/` depending on scope. See `examples/theory/` and `KB_CONVENTIONS.md`.
