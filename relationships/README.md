# Relationships

Supported variable-to-variable relationship pages go here. Use one page per variable pair to capture evidence, interpretation, and caveats. See `examples/relationships/` and `KB_CONVENTIONS.md`.
