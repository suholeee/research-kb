# Variables

Normalized variable pages go here. This layer tracks recurring observables, notation conflicts, units, and measurement differences across papers. See `examples/variables/` and `KB_CONVENTIONS.md`.
